[project]
name = "evidence"
version = "0.1.0"
description = "Evidence and finding models for AI Data Analyst"
dependencies = [
    "pydantic>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
