[project]
name = "ingestion"
version = "0.1.0"
description = "Ingestion pipeline package for AI Data Analyst"
dependencies = [
    "polars>=0.20.0",
    "pyarrow>=14.0.0",
    "openpyxl>=3.1.0",
    "xlrd>=2.0.1",
    "pydantic>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
