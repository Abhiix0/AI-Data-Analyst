[project]
name = "agent"
version = "0.1.0"
description = "Analytical agent package for AI Data Analyst"
dependencies = [
    "langgraph>=0.0.30",
    "pydantic>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
