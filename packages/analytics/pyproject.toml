[project]
name = "analytics"
version = "0.1.0"
description = "Deterministic analytics tool package for AI Data Analyst"
dependencies = [
    "polars>=0.20.0",
    "duckdb>=0.10.0",
    "pydantic>=2.5.0",
    "pyarrow>=14.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
