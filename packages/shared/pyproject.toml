[project]
name = "shared"
version = "0.1.0"
description = "Shared utilities and abstractions for AI Data Analyst"
dependencies = [
    "pydantic>=2.5.0",
    "boto3>=1.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
